version: '3.8'

services:
  backend:
    build:
      context: ./backend
    ports:
      - "8501:8501"
    volumes:
      - ./backend/uploads:/app/uploads
      - ./backend/db:/app/db
      - ./backend:/app
    environment:
      - API_KEY=${API_KEY}
    restart: always
